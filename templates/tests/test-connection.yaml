apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rustdesk-helm.fullname" . }}-test-connection"
  labels:
    {{- include "rustdesk-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rustdesk-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
