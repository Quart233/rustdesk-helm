apiVersion: v1
kind: Service
metadata:
  name: {{ include "rustdesk-helm.fullname" . }}
  labels:
    {{- include "rustdesk-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rustdesk-helm.selectorLabels" . | nindent 4 }}
